<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shoptext">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopText">
        <activity android:name=".home.homelistener.HomeDetailsFifthActivity"></activity>
        <activity android:name=".classification.view.ClassificationListActivity" />
        <activity android:name=".register.view.RegisterActivity" />
        <activity android:name=".detailslist.view.DetailsContentActivity" />
        <activity android:name=".detailslist.view.DetailsListActivity" />
        <activity android:name=".mine.view.activity.LoginHomeActivity" />
        <activity android:name=".mine.view.activity.RefreshTokenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>